<form method="post" action="{{ path('fees_value_delete', {'id': fees_value.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fees_value.id) }}">
    <button class="btn">Delete</button>
</form>
