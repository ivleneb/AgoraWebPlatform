<form method="post" action="{{ path('member_donation_delete', {'id': member_donation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ member_donation.id) }}">
    <button class="btn">Delete</button>
</form>
